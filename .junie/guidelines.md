# Junie guidelines for `lst-crc`

The instructions for coding agents are in [`CLAUDE.md`](../CLAUDE.md) at the repository root: build and test commands, rules that must not be broken, how to run the UI test workflows, and an architecture overview. More detail is in [`docs/`](../docs/README.md). Read those files. `CLAUDE.md` is the single source of truth, so don't add instructions here.
